# GPS Visualizationtool
A tool to visualize and trace gps data extracted from csv files

## How to run
To install the necessary packages run the command "npm install"

To run the program either use the command "npm start" or "node app.js"
